<!DOCTYPE html>
<html>
<head>
	<meta charset="UTF-8">
	<title>Insert title here</title>
	<style>
		h1{
			display : none;
		}	
	</style>
	<script src="https://code.jquery.com/jquery-3.6.0.js"></script>
	<script>
		$(document).ready(function(){
			/* 
			$('h1').eq(0).slideDown(2000).slideUp(2000);	// 반대말 hide, fadeIn, fadeOut, slideDown, slideUp
			$('h1').eq(1).fadeIn(2000);
			$('h1').eq(1).css('color', 'blue');
			 */
			
			// 3초에 "Hello JQUERY"문장을 보인 후, 글자색을 파란색으로 변경한 후,
			// 슬라이드효과로 올라갔다 내려갔다 수행한 후, 배경색을 노란색, 글자색은 빨간색으로 변경
			$('h1').eq(0).show(3000, function(){
				$(this).css('color','blue').slideUp(2000).slideDown(2000, function(){
					$(this).css({'background-color' : 'yellow', 'color' : 'red'})
				});
			})
			
			
		})
	</script>
</head>
<body>
	<h1>Hello jquery</h1>
	<h1>Hello jquery2</h1>
</body>
</html>